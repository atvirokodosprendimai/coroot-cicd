need to launch coroot server on hz vps, using domain: table.beerpub.dev

- [ ] Configure remote backups — set up off-site backup storage for Coroot volume snapshots (currently backups are only stored locally on the VPS at /opt/coroot/backups/)
